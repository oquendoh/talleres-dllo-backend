//Basic js

/*
- Se puede usar o no el ;
 */

//Declarar e inicializar variable
let variable1 = 1
//Debugging
console.log(typeof variable1) //"number"

let variable2 = "1"
console.log(typeof variable2) //"string"

//Declarar una constante (no va a cambiar a lo largo del programa)
const constante = 1

//Funciones (funcionas como variables)
function suma(a, b) {
    return a + b
}

console.log(suma(1, 2)) //3
